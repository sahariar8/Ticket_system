# What is jsx and why is it used?

## jsx means javascript XML. it looks like html but not exactly.we can write html like code within javascript.

# What is the difference between state and props?

## state changeable but props not changeable .props means parameter.pass parent to child component but state scope only that component.

# what is the useState hook,and how does it work?

## useState is eact built in hook,we can manage state by using this hook.in this hook ,we find a variable which hold the initial value and a setter function.when we need to change the state,we can change by using setter function.